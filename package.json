{
  "type": "module",
  "name": "to-typed",
  "version": "0.1.10",
  "description": "Type-guards, casts and converts unknowns into typed values",
  "main": "./dist/lib/index.js",
  "scripts": {
    "test": "tsc --build --clean & tsc --build & node ./dist/tests/all-tests.js",
    "build": "tsc --build --clean & tsc --build"
  },
  "author": "Juan Soldi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.35"
  }
}
